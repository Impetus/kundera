package com.impetus.kundera.graph;

public final class GraphGenerator
{

    
    /*
     * Responsible for:
     * 1. Do prechecks
     *                 a) validation entity
     *                 b) set id
     *                 c) check for shared via primary key
     *                 d) dirty check
     *
     * 2. Parse through relations and relational objects. 
     * 
     * 3. Recursively process transitive relations 
     *                  
     */
}
